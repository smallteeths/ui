/* eslint-env node */
'use strict';

module.exports = function(environment) {
  var ENV = {
    modulePrefix: 'custom-extension',
    environment: environment,
    APP: {
    },
  };

  return ENV;
};
